import React from 'react';

//here, we create a context object names 'UserContext' using React.createContext(). This context will be used to share user-realted data accross components




const UserContext = React.createContext();

export default UserContext;